/target
/.dev-data
web/node_modules
web/dist
*.db
*.db-wal
*.db-shm
web/coverage

# Incremental typecheck state: regenerated by every `tsc`, so it only ever
# arrives as noise in a diff.
web/tsconfig.tsbuildinfo

# What the journal-metrics plugin has already asked OpenAlex. Written at run
# time, different on every machine, and rebuilt on demand — so it is noise in
# a diff rather than something to ship.
plugins/journal-metrics/cache.json
