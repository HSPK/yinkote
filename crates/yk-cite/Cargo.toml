[package]
name = "yk-cite"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rendering citations and bibliographies"

[dependencies]
yk-core.workspace = true
serde.workspace = true
# For CSL-JSON. The rest of this crate renders text; see `export.rs` for why
# interchange formats live beside citation styles rather than inside them.
serde_json.workspace = true
